# DensityReportFull { #dyf.DensityReportFull }

```python
DensityReportFull(
    corpus_size,
    num_buckets,
    mean_bucket_size,
    median_bucket_size,
    max_bucket_size,
    mean_centroid_similarity,
    mean_isolation_score,
    mean_stability_score,
    pca_variance_explained,
    category_counts,
)
```

Report on density classification.